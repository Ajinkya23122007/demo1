#python program to genarate a random number on dice

import random

num=random.randint(1,6)
print(num)

#python program to print all prime number a interval
lower=int(input("enter a lower limit"))
upper=int(input("enter a upper limit"))
for num in range(lower,upper):
    if num>1:
        for i in range (2,num):
            if num%i==0:
                break
            else:
                print(num)